"use client";
import { useEffect, useMemo, useState } from "react"; import { ClipboardList, RefreshCw, UserCheck, Users } from "lucide-react"; import { addIssueNote, assignIssueAsOperator, listExperts, listOperatorIssues, updateIssuePriority, updateIssueStatus, type Expert, type Issue, type IssuePriority, type IssueStatus } from "@/services"; import { Card, DashboardShell, EmptyState, LoadingState, StatCard, StatusPill } from "@/components/ui-kit"; import { IssueDetailPanel } from "@/components/issue-detail-panel";
const statuses:IssueStatus[]=["operator_review","need_more_info","assigned","in_progress","completed","closed","rejected"]; const priorities:IssuePriority[]=["low","medium","high","critical"];
export default function OperatorPage(){ const [issues,setIssues]=useState<Issue[]>([]); const [experts,setExperts]=useState<Expert[]>([]); const [loading,setLoading]=useState(true); const [search,setSearch]=useState(""); const [note,setNote]=useState<Record<string,string>>({}); const [message,setMessage]=useState(""); async function refresh(){setLoading(true); try{const [i,e]=await Promise.allSettled([listOperatorIssues(),listExperts({available:true})]); if(i.status==="fulfilled")setIssues(i.value); if(e.status==="fulfilled")setExperts(e.value);}finally{setLoading(false);}} useEffect(()=>{refresh();},[]); async function action(work:Promise<unknown>, ok:string){try{await work; setMessage(ok); await refresh();}catch(e){setMessage(e instanceof Error?e.message:"Action failed");}} const filtered=useMemo(()=>issues.filter(i=>[i.title,i.description,i.status,i.category].join(" ").toLowerCase().includes(search.toLowerCase())),[issues,search]); return <DashboardShell title="Operator dashboard" subtitle="Manage issue review, priority, notes, and expert assignment." action={<button onClick={refresh} className="inline-flex items-center gap-2 rounded-xl border px-3 py-2 text-sm font-bold"><RefreshCw size={15}/>Refresh</button>}><div className="grid gap-4 md:grid-cols-3"><StatCard label="Queue" value={issues.length} icon={<ClipboardList size={20}/>}/><StatCard label="Open" value={issues.filter(i=>!["completed","closed","rejected"].includes(i.status)).length} icon={<UserCheck size={20}/>}/><StatCard label="Available experts" value={experts.length} icon={<Users size={20}/>}/></div>{message&&<p className="mt-4 rounded-xl bg-white/70 p-3 text-sm font-bold text-slate-600">{message}</p>}<input className="input mt-6" placeholder="Search queue" value={search} onChange={e=>setSearch(e.target.value)}/><div className="mt-6 space-y-4">{loading?<LoadingState/>:filtered.length===0?<EmptyState title="No issues in queue" text="No operator issues were returned by the backend."/>:filtered.map(issue=><Card key={issue.id} className="p-5"><div className="flex flex-col justify-between gap-3 md:flex-row"><div><h3 className="text-lg font-black">{issue.title}</h3><p className="mt-1 text-sm text-slate-500">{issue.description}</p><p className="mt-2 text-xs text-slate-500">{issue.location} {issue.pinCode}</p></div><StatusPill tone="info">{issue.status.replaceAll("_"," ")}</StatusPill></div><div className="mt-4 grid gap-3 md:grid-cols-3"><select className="input" value={issue.status} onChange={e=>action(updateIssueStatus(issue.id,e.target.value as IssueStatus),"Status updated")}>{statuses.map(s=><option key={s} value={s}>{s}</option>)}</select><select className="input" value={issue.priority||"medium"} onChange={e=>action(updateIssuePriority(issue.id,e.target.value as IssuePriority),"Priority updated")}>{priorities.map(p=><option key={p} value={p}>{p}</option>)}</select><select className="input" defaultValue="" onChange={e=>e.target.value&&action(assignIssueAsOperator(issue.id,e.target.value),"Expert assigned")}><option value="">Assign expert</option>{experts.map(ex=><option key={ex.id} value={ex.id}>{ex.fullName}</option>)}</select></div><IssueDetailPanel issue={issue} role="operator" onChanged={refresh}/><div className="mt-3 flex gap-2"><input className="input" placeholder="Operator note" value={note[issue.id]||""} onChange={e=>setNote({...note,[issue.id]:e.target.value})}/><button onClick={()=>note[issue.id]?.trim()&&action(addIssueNote(issue.id,note[issue.id]),"Note saved")} className="rounded-xl bg-slate-950 px-4 py-2 text-sm font-bold text-white">Save</button></div></Card>)}</div></DashboardShell> }
