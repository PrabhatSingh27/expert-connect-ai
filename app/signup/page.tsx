"use client";
import Link from "next/link"; import { useRouter } from "next/navigation"; import { useState } from "react"; import { register } from "@/services"; import { Brand, Card, PageShell } from "@/components/ui-kit";
export default function SignUpPage() { const router = useRouter(); const [accountType,setAccountType]=useState<"customer"|"expert">("customer"); const [form,setForm]=useState({name:"",email:"",password:"",phoneNumber:""}); const [photo,setPhoto]=useState<File>(); const [loading,setLoading]=useState(false); const [message,setMessage]=useState(""); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); if(accountType==="expert"){router.push("/become-technician");return;} setLoading(true); setMessage(""); try { await register({...form,photo}); router.push("/login"); } catch(err) { setMessage(err instanceof Error ? err.message : "Registration failed"); } finally { setLoading(false); } } return <PageShell><div className="grid min-h-screen place-items-center px-4 py-10"><Card className="w-full max-w-md p-6"><div className="mb-8"><Brand/></div><h1 className="text-3xl font-black">Create account</h1><form onSubmit={submit} className="mt-7 space-y-4"><select className="input" value={accountType} onChange={e=>setAccountType(e.target.value as "customer"|"expert")}><option value="customer">Customer</option><option value="expert">Expert</option></select>{accountType==="expert"?<p className="rounded-xl bg-teal-50 p-3 text-sm text-teal-800">Expert registration needs service and ID details.</p>:<><input className="input" placeholder="Full name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/><input className="input" type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/><input className="input" type="tel" placeholder="Phone number" value={form.phoneNumber} onChange={e=>setForm({...form,phoneNumber:e.target.value})} required/><input className="input" type="password" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} minLength={8} required/><input className="input" type="file" accept="image/*" onChange={e=>setPhoto(e.target.files?.[0])}/></>}<button disabled={loading} className="w-full rounded-2xl bg-slate-950 px-5 py-3 font-black text-white">{accountType==="expert"?"Continue as expert":loading?"Creating...":"Register"}</button></form>{message && <p className="mt-4 text-sm text-rose-600">{message}</p>}<p className="mt-5 text-sm">Already have account? <Link href="/login" className="font-bold text-teal-700">Login</Link></p></Card></div></PageShell>; }
